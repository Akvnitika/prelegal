# Copy to .env and fill in real values (.env is gitignored).
# The keys are passed into the Docker container by the start scripts and
# will be used by AI features in upcoming tickets.
OPENROUTER_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
