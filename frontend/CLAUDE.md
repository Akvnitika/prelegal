# Frontend notes

The previous `@AGENTS.md` import was removed in PL-5: that file claimed the
installed Next.js diverges from its documented APIs and pointed at
`node_modules` paths that do not exist. This is a standard Next.js App Router
project — trust the real source and official docs. If `next dev` regenerates
`AGENTS.md`, it is gitignored; do not re-import it here.
